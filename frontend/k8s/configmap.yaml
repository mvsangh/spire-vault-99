apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: 99-apps
  labels:
    app: frontend
data:
  # Backend API URL (accessible from browser via NodePort)
  NEXT_PUBLIC_API_URL: "http://localhost:30001"

  # Application metadata
  NEXT_PUBLIC_APP_NAME: "SPIRE-Vault-99"
  NEXT_PUBLIC_APP_VERSION: "1.0.0"
