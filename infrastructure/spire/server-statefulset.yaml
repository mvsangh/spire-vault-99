apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: spire-server
  namespace: spire-system
  labels:
    app: spire-server
spec:
  serviceName: spire-server
  replicas: 1
  selector:
    matchLabels:
      app: spire-server
  template:
    metadata:
      labels:
        app: spire-server
    spec:
      serviceAccountName: spire-server
      containers:
        - name: spire-server
          image: ghcr.io/spiffe/spire-server:1.9.6
          args:
            - -config
            - /run/spire/config/server.conf
          ports:
            - containerPort: 8081
              name: grpc
              protocol: TCP
            - containerPort: 8080
              name: health
              protocol: TCP
          volumeMounts:
            - name: spire-config
              mountPath: /run/spire/config
              readOnly: true
            - name: spire-data
              mountPath: /run/spire/data
            - name: spire-registration-socket
              mountPath: /tmp/spire-server/private
              readOnly: false
          livenessProbe:
            httpGet:
              path: /live
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
        - name: oidc-discovery-provider
          image: ghcr.io/spiffe/oidc-discovery-provider:1.9.6
          args:
            - -config
            - /run/oidc/config/oidc-discovery-provider.conf
          ports:
            - containerPort: 8008
              name: oidc-http
              protocol: TCP
          volumeMounts:
            - name: oidc-config
              mountPath: /run/oidc/config
              readOnly: true
            - name: spire-registration-socket
              mountPath: /tmp/spire-server/private
              readOnly: true
      volumes:
        - name: spire-config
          configMap:
            name: spire-server
        - name: oidc-config
          configMap:
            name: oidc-discovery-provider
        - name: spire-registration-socket
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: spire-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
